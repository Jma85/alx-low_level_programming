#include <stdio.h>
#include <ctype.h>

/**
 * decrition : msin - program ptints alphabet in lowercase
 * return : 0
*/

int main(void)
{
int n = 'a'
while(n <= 'z')
{
putchar(n);
n += 1;
}
putchar('\n');
return (0);
}

